#pragma once
#include "graph.hpp"
namespace graphTest {

    template<typename dataT>
    bool hasCycle(Graph<dataT> *g){
        // DFS 三色标记法 for 有向图
        // TODO
    }

    template<typename dataT>
    bool isSame(Graph<dataT> *g1, Graph<dataT> *g2){
        // TODO
    }

}